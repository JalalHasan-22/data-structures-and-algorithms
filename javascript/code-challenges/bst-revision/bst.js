"use strict";

const Node = require("./node");

class BinarySearchTree {
  constructor(root = null) {
    this.root = root;
  }

  // Traversing the tree
  // 1) Depth First traversal
  // a) pre-order
  preOrder() {}
}
